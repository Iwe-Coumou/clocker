{
  "name": "clocker",
  "version": "1.5.0",
  "private": true,
  "description": "Self-hosted weekly hours timecard, balanced across the month, with persistent file-backed storage.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}

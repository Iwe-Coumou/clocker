# Copy this file to .env and set real values to require a login.
# Leave both blank (or don't create .env at all) to run with no auth.
#
#   cp .env.example .env
#
# .env is gitignored — never commit real credentials.

AUTH_USER=
AUTH_PASS=
